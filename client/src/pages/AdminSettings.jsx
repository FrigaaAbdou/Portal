import AccountLayout from '../components/layout/AccountLayout'

export default function AdminSettings() {
  return (
    <AccountLayout title="Admin Settings">
      <p className="text-sm text-gray-600">
        Configure admin preferences, notifications, and access controls. (Placeholder view until settings UI is built.)
      </p>
    </AccountLayout>
  )
}
