import AccountLayout from '../components/layout/AccountLayout'

export default function AdminWebhooks() {
  return (
    <AccountLayout title="Webhook Logs">
      <p className="text-sm text-gray-600">
        Monitor incoming Stripe webhooks and troubleshoot failures. (Placeholder view until logging UI is built.)
      </p>
    </AccountLayout>
  )
}
